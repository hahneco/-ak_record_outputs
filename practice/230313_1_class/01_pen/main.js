'use strict'

/*

// penオブジェクト
let pen = {
  color: "red",
  length: 5,
  draw: function (d) {
    this.length -= 0.01 * d; // penのlengthが削れる
  }
};
function stroke() {
  pen.draw(3); // penオブジェクトのdrawメソッドを実行 // オブジェクトのプロパティ参照=「オブジェクト.プロパティ名」
  // console.log(pen.draw);
  document.getElementById("length").textContent = pen.length
}

*/
